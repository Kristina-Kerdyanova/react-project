import styled from "styled-components";

const StyledFormMap = styled.form``;

const StyledSelectMap = styled.select``;

const StyledOptionMap = styled.option``;

export { StyledFormMap, StyledOptionMap, StyledSelectMap };
